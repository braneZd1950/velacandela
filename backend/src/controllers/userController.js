export async function register(_req, res) {
  res.status(501).json({ error: 'Registracija — implementirajte prema dokumentaciji.' })
}

export async function login(_req, res) {
  res.status(501).json({ error: 'Login — implementirajte prema dokumentaciji.' })
}

export async function getMe(_req, res) {
  res.status(501).json({ error: 'Profil — implementirajte prema dokumentaciji.' })
}

export async function updateMe(_req, res) {
  res.status(501).json({ error: 'Uređivanje profila — implementirajte prema dokumentaciji.' })
}
